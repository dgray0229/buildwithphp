{% if flash is not empty %}
  {% for key, message in flash %}
    <div id="feedback" class="{{ key }}">
      <a class="dismiss">Dismiss</a>
      <h3>{{ key }}</h3>
      <p>
        {{ message }}
      </p>
    </div>
  {% endfor %}
{% endif %}
